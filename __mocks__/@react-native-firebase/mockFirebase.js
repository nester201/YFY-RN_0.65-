jest.mock('@react-native-firebase/app', () => null);

jest.mock('@react-native-firebase/analytics', () => null);

jest.mock('@react-native-firebase/auth', () => null);

jest.mock('@react-native-firebase/storage', () => null);

jest.mock('@react-native-community/google-signin', () => null);
